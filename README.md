# worker-project-lerna-repository

Let's pull the latest MongoDB image
docker pull mongo

Now, let's create a dedicated folders in C: drive

C:\mongodb
C:\mongodb\db

Add lerna global
yarn global add lerna

Next
yarn && yarn build
